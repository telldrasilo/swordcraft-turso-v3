/**
 * Сегмент реестра: добываемые узлы мира (`worldResourceNodes`).
 */

import { worldResourceNodes } from './world-resource-nodes'
import type { MaterialNode } from '@/types/materials/material-core'

export const registryWorldMaterialNodes: MaterialNode[] = worldResourceNodes
