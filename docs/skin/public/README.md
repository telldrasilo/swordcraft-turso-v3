# Статика (`public/`)

Файлы из этой папки отдаются с **корня** URL: `http://localhost:3010/имя-файла.png`.

Используйте для макетов: фоны, иконки-черновики, JSON-заглушки. В разметке указывайте `/logo.png`, не `public/logo.png`.
